# When loading wsl, bashrc is not loaded.
# https://github.com/microsoft/WSL/issues/2067

if [[ -f ~/.bashrc ]] ; then
  source ~/.bashrc
fi

if [[ -f ~/projects/dotfiles/personal.bashrc ]] ; then
  source ~/projects/dotfiles/personal.bashrc
fi

export PATH=~/bin:$PATH
